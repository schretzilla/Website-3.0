language: node_js
node_js:
  - "stable"
cache:
  directories:
    - node_modules

jobs:
  include:
    - stage: "Quality-Checks"
      name: "Linting"
    - script: npm run lint
      name: "Testing"
    - script: npm test
    - stage: "Build"
      script: npm run build
