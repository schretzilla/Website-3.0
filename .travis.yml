language: node_js
node_js:
  - "stable"
cache:
  directories:
    - node_modules

jobs:
  include:
    - stage: "Quality-Checks"
      script: npm run lint
    - script: npm run test
    - stage: "Build"
      script: npm run build
